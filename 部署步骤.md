# 上线步骤：GitHub → Cloudflare Pages → 绑定域名

整个过程约 10 分钟，全程在浏览器里点，不需要写命令。
你要上传的文件就是这个文件夹里的 **index.html**。

---

## 第一步：把文件传到 GitHub

1. 打开 https://github.com/new （已登录的话直接到新建仓库页）
2. **Repository name** 填一个名字，例如 `life-planner`
3. 选 **Public**（公开，免费够用）
4. 不要勾 "Add a README"，直接点 **Create repository**
5. 进入空仓库页面后，点中间的链接 **uploading an existing file**
   （或直接打开 `https://github.com/你的用户名/life-planner/upload/main`）
6. 把本文件夹里的 **index.html** 拖进上传框
7. 下方点绿色按钮 **Commit changes**

✅ 完成后，仓库里能看到 index.html 就对了。

---

## 第二步：在 Cloudflare Pages 上线

1. 打开 https://dash.cloudflare.com/ → 左侧菜单 **Workers & Pages**
2. 点 **Create application** → 选 **Pages** 标签 → **Connect to Git**
3. 第一次会让你授权 GitHub，点 **Connect GitHub** 并允许（这一步是把两个账号打通，安全）
4. 选中刚才的 `life-planner` 仓库 → **Begin setup**
5. 构建设置全部留空（因为是纯静态网页）：
   - Framework preset：**None**
   - Build command：**留空**
   - Build output directory：**留空**（或填 `/`）
6. 点 **Save and Deploy**

⏳ 等约 1 分钟，会得到一个网址：`life-planner-xxx.pages.dev`
点开就能用了。手机浏览器也能打开，可以"添加到主屏幕"当 App 用。

---

## 第三步：绑定你自己的域名

> 前提：你的域名最好已经托管在 Cloudflare（DNS 在 Cloudflare 这边）。
> 如果域名在别处（如阿里云、GoDaddy），见文末备注。

1. 在刚部署好的 Pages 项目里，点上方 **Custom domains** 标签
2. 点 **Set up a custom domain**
3. 输入你想用的域名，例如 `plan.你的域名.com` → **Continue**
4. 如果域名已在 Cloudflare，它会自动加好 DNS 记录，点 **Activate domain**
5. 等几分钟生效，之后用你的域名就能直接访问。

### 备注：域名不在 Cloudflare 怎么办
Cloudflare 会显示一条需要添加的 **CNAME 记录**（目标是 `xxx.pages.dev`）。
到你域名服务商的后台，加一条对应的 CNAME 记录即可。
这一步涉及账号设置，需要你自己操作；具体记录值贴给我，我可以帮你核对。

---

## 以后怎么更新网页内容
改完本文件夹的 index.html 后，重复"第一步"重新上传同名文件覆盖即可，
Cloudflare 会自动重新部署，几十秒后线上就更新了。

---

哪一步卡住了，把截图或报错发给我，我帮你看。
